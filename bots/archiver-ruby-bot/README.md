# Nutella Archiver Bot

This nutella bot archives all the messages exchanged in a certain run. It assumes that you have a MongoDB server running on 'localhost' because that's where the message will be store.
